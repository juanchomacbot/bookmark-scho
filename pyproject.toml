[project]
name = "bookmark-scho"
version = "0.1.0"
requires-python = ">= 3.12.13"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0,<9.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
